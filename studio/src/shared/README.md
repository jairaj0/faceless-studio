# shared/ — types shared between main (backend) and renderer (frontend)

Pure type definitions only (no runtime code that imports Electron or DOM).
Examples: ExportRequest, ExportProgress, ImportedMedia, IPC payload types.
