import type { Scene } from "./types";

/** Blank project — what the editor opens with. */
export const emptyScene: Scene = {
  width: 1920,
  height: 1080,
  fps: 30,
  duration: 5000,
  background: "#0b0f1a",
  layers: [],
};
